# ===============================================================
# PRODUK?NÍ KONFIGURACE (PROFIL: prod)
# Zam??eno na bezpe?nost, výkon a stabilitu
# ===============================================================

# --- DATABÁZE & HIBERNATE ---
# V produkci NIKDY nenecháváme Hibernate sahat na strukturu tabulek
# Pouze kontroluje, zda Java entity odpovídají DB. Pokud ne, aplikace nenastartuje (Fail Fast).
spring.jpa.hibernate.ddl-auto=validate

# Vypneme výpis SQL (zbyte?n? by plnil logy a zpomaloval appku)
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Flyway musí b??et, aby se aplikovaly migrace ?ízen?
spring.flyway.enabled=true

# --- PERFORMANCE TUNING (HIKARI CP) ---
# Lad?ní Connection Poolu pro vy??í zát??
# Minimální po?et spojení, která se udr?ují stále otev?ená (rychlej?í odezva)
spring.datasource.hikari.minimum-idle=5
# Maximální po?et spojení (zále?í na tom, co unese databáze, nap?. 20 je pro za?átek ok)
spring.datasource.hikari.maximum-pool-size=20
# Jak dlouho ?ekat na volné spojení, ne? vyhodí chybu (30s)
spring.datasource.hikari.connection-timeout=30000
# Maximální ?ivotnost spojení (nap?. 30 minut)
spring.datasource.hikari.max-lifetime=1800000

# --- LOGOVÁNÍ ---
# V produkci nás zajímají jen chyby (ERROR) a d?le?ité informace (INFO)
# ?ádné DEBUG nebo TRACE, jinak se logy nafouknou o gigabajty
logging.level.root=INFO
logging.level.com.checkfood=INFO
logging.level.org.springframework=WARN
logging.level.org.hibernate=ERROR

# --- EMAIL ---
# Vypnout lad?ní email?
spring.mail.properties.mail.debug=false
# Zde se spoléháme na to, ?e `ssl.trust` je nastaven v hlavním souboru na konkrétní host
# nebo spoléháme na certifikáty OS. ?ádné hv?zdi?ky (*) zde!

# --- OSTATNÍ ---
# Vypnout restarty (DevTools by na produkci nem?ly být, ale pro jistotu)
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false